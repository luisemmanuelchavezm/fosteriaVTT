import { describe, it, expect } from "vitest";

describe("Primer test de FosteriaVTT", () => {
  it("debería sumar 2 + 2 correctamente", () => {
    expect(2 + 2).toBe(4);
  });

  it("el entorno de Vitest está configurado", () => {
    expect(true).toBe(true);
  });
});
